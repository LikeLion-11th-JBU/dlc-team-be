<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content="" />
    <meta
      name="author"
      content="Mark Otto, Jacob Thornton, and Bootstrap contributors"
    />
    <meta name="generator" content="Hugo 0.101.0" />
    <title>회원가입</title>

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"
      integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N"
      crossorigin="anonymous"
    />

    <!-- Bootstrap core CSS -->

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
    </style>

    <!-- Custom styles for this template -->
    <link href="form-validation.css" rel="stylesheet" />
  </head>
  <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>

  <body class="bg-light">
    <div class="container">
      <div class="py-5 text-center">
        <h1>회원가입</h1>
      </div>
      <form action="/register" method="POST">
        <div class="col-md-8 order-md-1">
          <h4 class="mb-3">가입정보</h4>
          <form class="needs-validation" novalidate>
            <div class="col-md-6 mb-3">
              <input
                type="text"
                class="form-control"
                id="role"
                name="역할"
                required
                value="교육자"
                style="display: none"
              />
            </div>
            <div class="col-md-6 mb-3">
              <label>역할 선택</label><br />
              <input
                type="radio"
                name="roleOption"
                value="교육자"
                id="instructor"
                checked
              />
              <label for="instructor">교육자</label>
              <input
                type="radio"
                name="roleOption"
                value="교육생"
                id="student"
              />
              <label for="female">교육생</label>
            </div>
            <div class="row">
              <div class="col-md-6 mb-3">
                <label>이름</label>
                <input
                  type="text"
                  class="form-control"
                  id="name"
                  name="이름"
                  placeholder=""
                  required
                />
                <div class="invalid-feedback">Valid last name is required.</div>
              </div>
            </div>

            <div class="mb-3">
              <label>아이디</label>
              <div class="input-group">
                <div class="input-group-prepend"></div>
                <input
                  type="text"
                  class="form-control"
                  id="username"
                  name="아이디"
                  placeholder="아이디"
                  required
                />
                <div class="invalid-feedback" style="width: 100%">
                  Your username is required.
                </div>
                <button id="checkUsername">중복체크</button>
              </div>
            </div>
            <div class="mb-3">
              <label>비밀번호 <span class="text-muted"></span></label>
              <input
                type="password"
                class="form-control"
                id="password"
                name="비밀번호"
                placeholder="비밀번호"
              />
              <div class="invalid-feedback">
                Please enter a valid password for shipping updates.
              </div>
            </div>
            <div class="mb-3">
              <label>비밀번호 확인<span class="text-muted"></span></label>
              <input
                type="password"
                class="form-control"
                id="passwordAgain"
                name="비밀번호확인"
                placeholder="비밀번호확인"
              />
            </div>
            <div class="col-md-6 mb-3">
              <input
                type="text"
                class="form-control"
                id="gender"
                name="성별"
                required
                value="남성"
                style="display: none"
              />
            </div>
            <div class="col-md-6 mb-3">
              <label>성별 선택</label><br />
              <input
                type="radio"
                name="genderOption"
                value="남성"
                id="male"
                checked
              />
              <label for="male">남성</label>
              <input
                type="radio"
                name="genderOption"
                value="여성"
                id="female"
              />
              <label for="female">여성</label>
            </div>
            <div class="mb-3">
              <label>전화번호 <span class="text-muted"></span></label>
              <input
                type="text"
                class="form-control"
                id="homenumber"
                name="전화번호"
              />
              <div class="invalid-feedback">Please enter a valid</div>
            </div>
            <div class="mb-3">
              <label>핸드폰 <span class="text-muted"></span></label>
              <input
                type="text"
                class="form-control"
                id="phonenumber
                "
                name="핸드폰"
                placeholder="010-xxxx-xxxx"
              />
              <div class="invalid-feedback">Please enter a valid</div>
            </div>
            <div class="mb-3">
              <label>이메일 <span class="text-muted"></span></label>
              <input
                type="email"
                class="form-control"
                id="email"
                name="이메일"
                placeholder="you@example.com"
              />
              <div class="invalid-feedback">
                Please enter a valid email address for shipping updates.
              </div>
            </div>
            <button
              class="btn btn-primary btn-lg btn-block"
              type="submit"
              id="registerButton"
            >
              회원가입하기
            </button>
          </form>
        </div>
      </form>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-Fy6S3B9q64WdZWQUiU+q4/2Lc9npb8tCaSX9FK7E8HnRr0Jz8D6OP9dO5Vg3Q9ct"
      crossorigin="anonymous"
    ></script>

    <script>
      //성별 입력
      const genderInput = document.getElementById('gender')
      const maleRadio = document.getElementById('male')
      const femaleRadio = document.getElementById('female')

      // 남성 라디오 버튼 클릭 시
      maleRadio.addEventListener('click', () => {
        genderInput.value = '남성'
      })

      // 여성 라디오 버튼 클릭 시
      femaleRadio.addEventListener('click', () => {
        genderInput.value = '여성'
      })

      //역할 입력
      const roleInput = document.getElementById('role')
      const instructorRadio = document.getElementById('instructor')
      const studentRadio = document.getElementById('student')

      // 교육자 라디오 버튼 클릭 시
      instructorRadio.addEventListener('click', () => {
        roleInput.value = '교육자'
      })

      // 교육생 라디오 버튼 클릭 시
      studentRadio.addEventListener('click', () => {
        roleInput.value = '교육생'
      })

      ////중복체크
      const usernameInput = document.getElementById('username')
      const checkButton = document.getElementById('checkUsername')

      // 중복체크 버튼 클릭 이벤트 핸들러
      checkButton.addEventListener('click', () => {
        const username = usernameInput.value
        checkDuplicateUsername(username)
      })

      // 아이디 중복체크 함수
      function checkDuplicateUsername(username) {
        event.preventDefault()
        fetch('/checkUsername', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ username }),
        })
          .then((response) => response.json())
          .then((data) => {
            if (data.duplicate) {
              alert('이미 사용 중인 아이디입니다.')
            } else {
              alert('사용 가능한 아이디입니다.')
            }
          })
          .catch((error) => {
            console.error('Error:', error)
            alert('서버와의 통신에 문제가 발생했습니다.')
          })
      }
      ////비밀번호확인 기능
      const passwordInput = document.getElementById('password')
      const passwordAgainInput = document.getElementById('passwordAgain')
      const registerButton = document.getElementById('registerButton')

      registerButton.addEventListener('click', (event) => {
        const password = passwordInput.value
        const passwordAgain = passwordAgainInput.value

        if (password !== passwordAgain) {
          event.preventDefault()
          alert('비밀번호와 비밀번호 확인이 일치하지 않습니다.')
        }
      })
    </script>
  </body>
</html>
